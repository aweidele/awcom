<div class="row">
  <div class="col">
    <ul class="jobs">
    {% for job in section.jobs %}
      <li>
        <h3>
          <span>{{ job.years }}</span>
          <span>{{ job.company }}</span>
        </h3>
        <div>{{ job.years }}</div>
      </li>
    {% endfor %}
    </ul>
  </div>
</div>
