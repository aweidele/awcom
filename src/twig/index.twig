<!DOCTYPE HTML>
<html>
<head>
  <title>{{title}}</title>
</head>
<body>
  <h1>{{title}}</h1>
  <p>{{subtitle}}</p>
  <ul>
    {% for work in experience %}
      <li>{{work.year}} - <a href="{{work.url}}" target="_blank">{{work.company}}</a> - {{work.position}}</li>
    {% endfor %}
  </ul>
</body>
</html>
