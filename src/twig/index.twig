{% extends "templates/layouts/layout.twig" %}
{% block page %}
  <header id="top" class="{{ content.sections.top.layout }}">
    {% include "templates/components/intro.twig" with {
      "content": content.sections.top
    } %}
  </header>
  {% include "templates/components/section.twig" with {
    "section": content.sections.web
  } %}
  {# % include "templates/components/section.twig" with {
    "section": content.sections.cv
  } % #}
{% endblock %}
